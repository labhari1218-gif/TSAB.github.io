<div class="ambient-backdrop" aria-hidden="true">
  <div class="backdrop-orb orb-a"></div>
  <div class="backdrop-orb orb-b"></div>
  <div class="backdrop-mesh"></div>
</div>

<style>
  .ambient-backdrop {
    position: fixed;
    inset: 0;
    z-index: -3;
    overflow: hidden;
    pointer-events: none;
  }

  .backdrop-orb {
    position: absolute;
    border-radius: 999px;
    filter: blur(8px);
    opacity: 0.8;
  }

  .orb-a {
    width: 32rem;
    height: 32rem;
    left: -8rem;
    top: 8rem;
    background: radial-gradient(circle, rgba(214, 163, 58, 0.14), transparent 70%);
  }

  .orb-b {
    width: 38rem;
    height: 38rem;
    right: -10rem;
    top: 18rem;
    background: radial-gradient(circle, rgba(21, 58, 120, 0.16), transparent 72%);
  }

  .backdrop-mesh {
    position: absolute;
    inset: 0;
    opacity: 0.08;
    background-image:
      linear-gradient(rgba(21, 58, 120, 0.35) 1px, transparent 1px),
      linear-gradient(90deg, rgba(21, 58, 120, 0.28) 1px, transparent 1px);
    background-size: 40px 40px;
    mask-image: linear-gradient(180deg, transparent 0, rgba(0, 0, 0, 0.8) 18%, rgba(0, 0, 0, 0.35) 88%, transparent 100%);
  }
</style>
