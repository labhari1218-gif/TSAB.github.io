---
import { withBase } from "@/lib/site";

interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a class="button secondary" href={withBase(href)}>{label}</a>
