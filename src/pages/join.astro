---
import CalloutPanel from "@/components/CalloutPanel.astro";
import SectionIntro from "@/components/SectionIntro.astro";
import MainLayout from "@/layouts/MainLayout.astro";
import { getSiteSettings } from "@/lib/settings";
import { withBase } from "@/lib/site";

const settings = await getSiteSettings();
---

<MainLayout title="Join TSAB">
  <section class="page-section">
    <div class="shell">
      <SectionIntro
        eyebrow="Join / Contact"
        title="Joining TSAB should feel simple, warm, and easy to understand."
        copy="Whether you want to join the community, volunteer, submit media, or nominate an achiever, this page should make the next step clear."
      />
      <div class="listing-grid two-up action-grid">
        {settings.joinActions.map((action, index) => (
          <article class="card action-card">
            <p class="action-label">{index === 0 ? "Start Here" : "Public Pathway"}</p>
            <h2>{action.title}</h2>
            <p>{action.description}</p>
            {action.placeholder && <p class="placeholder-note">This is a placeholder action. Replace the public link when TSAB finalizes it.</p>}
            <a class:list={["button", index === 0 ? "primary" : "secondary"]} href={withBase(action.href)}>
              {action.label}
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="page-section">
    <div class="shell">
      <CalloutPanel
        eyebrow="Contact"
        title="Keep the path public, even while some links are still being finalized."
        copy={`The current public email is ${settings.email}. Replace the remaining placeholder links in the site settings as soon as TSAB has final forms and join URLs.`}
        primaryHref={`mailto:${settings.email}`}
        primaryLabel="Email TSAB"
        secondaryHref="/working-groups/"
        secondaryLabel="See Working Groups"
      />
    </div>
  </section>
</MainLayout>

<style>
  .action-grid {
    margin-top: 2rem;
  }

  .action-card {
    padding: 1.5rem;
  }

  .action-card h2 {
    margin: 0 0 0.6rem;
    font-family: "Cormorant Garamond", serif;
    font-size: 1.9rem;
  }

  .action-label {
    margin: 0 0 0.55rem;
    color: var(--color-brand);
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .action-card p {
    color: var(--color-muted);
  }

  .placeholder-note {
    color: var(--color-brand);
    font-size: 0.9rem;
    font-weight: 600;
  }
</style>
