---
import CalloutPanel from "@/components/CalloutPanel.astro";
import SectionIntro from "@/components/SectionIntro.astro";
import MainLayout from "@/layouts/MainLayout.astro";
import { getSiteSettings } from "@/lib/settings";

const settings = await getSiteSettings();
---

<MainLayout title="About TSAB">
  <section class="page-section">
    <div class="shell split-hero">
      <div class="surface-panel ambient-grid" data-spotlight data-reveal>
        <div class="eyebrow">{settings.aboutIntro.eyebrow}</div>
        <h1>{settings.aboutIntro.title}</h1>
        <p class="accent-te">{settings.aboutIntro.accentTe}</p>
        {settings.aboutIntro.body.map((paragraph) => <p>{paragraph}</p>)}
      </div>
      <div class="card timeline-card ambient-grid" data-spotlight data-tilt data-reveal data-reveal-delay="1">
        <p class="eyebrow">Mission</p>
        <ol>
          {settings.aboutMissionPoints.map((point) => <li>{point}</li>)}
        </ol>
      </div>
    </div>
  </section>

  <section class="page-section">
    <div class="shell">
      <SectionIntro
        eyebrow="Mission"
        title="A trustworthy space for culture, support, opportunities, and shared student momentum."
        copy="TSAB is not only about events. It is a community platform that helps Telugu students in Bhopal stay connected, supported, represented, and encouraged to grow."
      />
      <div class="listing-grid two-up about-grid">
        <article class="card prose-panel" data-spotlight data-tilt data-reveal>
          <h2>{settings.aboutIntro.whoCanJoinTitle}</h2>
          <p>{settings.aboutIntro.whoCanJoinBody}</p>
        </article>
        <article class="card prose-panel" data-spotlight data-tilt data-reveal data-reveal-delay="1">
          <h2>{settings.aboutIntro.familiarSpaceTitle}</h2>
          <p>{settings.aboutIntro.familiarSpaceBody}</p>
        </article>
      </div>
    </div>
  </section>

  <section class="page-section">
    <div class="shell">
      <div class="listing-grid three-up">
        <article class="card pillar-card" data-spotlight data-tilt data-reveal>
          <h2>Belonging</h2>
          <p>Help students feel at home quickly by connecting them to people, updates, and a familiar cultural space.</p>
        </article>
        <article class="card pillar-card" data-spotlight data-tilt data-reveal data-reveal-delay="1">
          <h2>Growth</h2>
          <p>Share opportunities, useful student resources, and achiever stories that encourage academic and professional growth.</p>
        </article>
        <article class="card pillar-card" data-spotlight data-tilt data-reveal data-reveal-delay="2">
          <h2>Culture</h2>
          <p>Celebrate Telugu festivals, language, identity, and heritage in a way that feels warm, modern, and visible.</p>
        </article>
      </div>
    </div>
  </section>

  <section class="page-section">
    <div class="shell">
      <CalloutPanel
        eyebrow="Next Step"
        title="TSAB can feel like a familiar space away from home."
        copy="Learn what TSAB stands for, why it exists, and how Telugu students in Bhopal can become part of a stronger support system."
        primaryHref="/join/"
        primaryLabel="Join TSAB"
        secondaryHref="/team/"
        secondaryLabel="Meet the Team"
      />
    </div>
  </section>
</MainLayout>

<style>
  h1 {
    margin: 0.8rem 0 0;
    max-width: 12ch;
    font-family: "Cormorant Garamond", serif;
    font-size: clamp(2.9rem, 6vw, 5rem);
    line-height: 0.92;
  }

  p,
  li {
    color: var(--color-muted);
  }

  .accent-te {
    margin: 0.35rem 0 1rem;
    color: var(--color-brand);
    font-weight: 700;
  }

  .timeline-card,
  .prose-panel,
  .pillar-card {
    padding: 1.5rem;
  }

  ol,
  ul {
    margin: 1rem 0 0;
    padding-left: 1.2rem;
  }

  .pillar-card h2,
  .prose-panel h2 {
    margin: 0 0 0.6rem;
    font-family: "Cormorant Garamond", serif;
    font-size: 1.9rem;
  }
</style>
